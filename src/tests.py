from tqdm import tqdm
import numpy as np
import time
o = tqdm(range(100), 'adfeafae..')
for i in o:
    time.sleep(1)
    o.set_description(str(np.random.normal()) + 'faefaefefeseszfeaseseszfesfesfesfesrfeszfres\n')